# CGS转台三环 PID 强化学习调参项目流程

## 1. 项目目标

项目面向“位置外环—速度中环—电流内环”的串级电机控制系统，并在速度/转矩通道加入 DOBC 扰动补偿。SAC 在物理电机仿真环境中搜索以下 11 个参数：

- 位置环：`kppos`、`kipos`、`kdpos`
- 速度环：`kpspeed`、`kispeed`、`kdspeed`
- DOBC：`kgspeed`、`tauspeed`
- 电流环：`kpcurr`、`kicurr`、`kdcurr`

强化学习输出的是经过仿真评价和鲁棒性审计的候选参数，不依赖预先存在的“最优参数标签”。

## 2. 数据与物理电机模型

当前唯一训练后端是 `physics`。电机由导师给出的数学模型和参数构造，包含电气方程、机械方程、三环 PIDF、DOBC、采样延迟、饱和、抗积分饱和、编码器量化/噪声以及负载扰动。

标称电机参数按配置的不确定性范围采样，形成固定且可复现的模型划分：

- 训练集：40 个物理电机，用于 SAC 在线交互和 reward 计算
- 验证集：16 个物理电机，用于候选筛选和完整审计
- 最终测试集：16 个全新域内电机和 8 个 OOD 压力测试电机

原始三环频响经清洗后保留 123 个实测点：电流环 18 点、六组速度环共 90 点、位置环 15 点。它们作为观测上下文、模型现实性对照和验证依据，不再用于拟合训练用代理电机。

## 3. SAC 交互流程

一次环境交互为：

```text
当前状态
  → SAC 输出 11 维动作
  → 映射为有上下界和单步幅度约束的物理参数
  → 在抽取的物理电机上运行闭环仿真
  → 计算频域、时域和安全指标
  → 生成 reward 与下一状态
```

环境在线生成 `(state, action, reward, next_state, terminated, truncated)` transition，并写入 SAC Replay Buffer。项目中的小型 transition 文件只用于格式验证和复现，不是监督学习标签，也不是正式训练集的上限。

训练可以按电流环、速度环、位置环、DOBC 和 11 维联合微调五个阶段进行；阶段动作掩码只允许修改当前阶段对应参数。

## 4. 评价器与 Reward

物理评价器同时计算：

- 频域：交越频率、带宽、相位裕度、增益裕度、灵敏度峰值、闭环稳定性和三环带宽层级
- 时域：上升时间、调节时间、超调量、稳态误差、IAE、控制峰值/变化率、扰动峰值和恢复时间
- 安全性：电压、电流、速度限制，仿真终止条件，以及训练/验证模型上的鲁棒稳定性

Reward 奖励综合成本相对上一步的改善，同时惩罚较大的绝对成本和动作幅度；不稳定、越限或审计失败会触发强负奖励并终止当前 episode。

## 5. 数据隔离与最终验收

训练集用于更新 SAC，验证集用于候选选择。最终测试集不参与 reward、早停、超参数选择或候选选择；正式训练结束后先锁定唯一候选，再运行一次最终测试。

通过最终仿真测试只能说明参数在当前物理模型和不确定性范围内表现较好，不能直接宣称为实机最优参数。进入硬件前仍需确认真实限幅、编码器规格和反馈滤波，并依次完成 HIL、保护条件下的低能量测试和实机验证。

## 6. 当前实现边界

项目当前已完成物理模型集合、11 维参数空间、频域/时域评价器、Gymnasium 环境、transition 生成、GPU SAC 冒烟检测、正式服务器训练程序以及训练/验证/测试隔离。历史上的频响代理电机、`plant_ensemble` 和代理评价器路线已从活动代码与数据中删除。

正式训练入口为 `scripts/train_sac.py`，配置为 `config/sac_training.json`。它与 `scripts/train_sac_smoke.py` 相互独立，支持五阶段课程、多随机种子、TensorBoard、定期验证、模型与 Replay Buffer checkpoint、正常终止信号处理和断点续训。

本机只完成短程工程验收，不进行长程训练。现有 SAC 输出仍属于流程验证结果，不代表正式长程训练已经收敛，也没有消耗封存的最终测试集。

## 7. 服务器正式训练与候选选择

服务器训练包由 `scripts/package_server_training.py` 构建，只包含正式训练源码、配置、40 个训练电机和 16 个验证电机，不包含封存测试集。上传服务器后先运行：

```powershell
python .\scripts\check_server_runtime.py --device cuda
```

随后为每个随机种子独立运行：

```powershell
python .\scripts\train_sac.py --seed 20260801 --device cuda
python .\scripts\train_sac.py --seed 20260802 --device cuda
python .\scripts\train_sac.py --seed 20260803 --device cuda
```

中断后使用相同种子和输出目录并添加 `--resume`。三个种子完成后运行 `scripts/select_final_candidate.py`，根据训练/验证审计生成唯一 `final_candidate.npz`。候选下载回本机并锁定后，才允许运行一次封存测试；封存测试结果不得用于重新训练或重新选择候选。
